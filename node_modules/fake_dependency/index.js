const raw = require('./config/default.json')

const config = require('config')

console.log('=== Now inside fake_dependency === ')
console.log(`Expected value of MyParam to be ${raw.MyParam}`)
console.log(`However, the value retrieved is ${config.get('MyParam')}`)
console.log('=== Leaving fake_dependency === ')

module.exports = {
}
